# 1. Chạy PySpark với Docker
* Mở terminal tại cùng cấp thư mục với `Dockerfile`.
* Dùng các lệnh sau:
  ```bash
  docker build -t manhcuong8499/test_image .
  docker run manhcuong8499/test_image
  ```